refactor: reorganize repo structure — frontend/ and docs/ directories

Move all frontend source files (components, hooks, services, utils,
contexts, config, lib, App.tsx, types.ts, constants.ts, index.tsx,
index.css) into frontend/ directory, parallel to backend/.

Consolidate loose root-level docs into docs/ subdirectories:
- docs/setup/ (QUICK_START, ENVIRONMENT_SETUP, OAUTH guides)
- docs/deployment/ (DEPLOYMENT.md)
- docs/ (DESIGN_LANGUAGE.md, SECURITY.md)

Remove stale files: root uv.lock, metadata.json,
backend/backend/requirements.txt. Untrack .env.production and
backend/family_meal_planner.db.

Update build config (vite, tsconfig, tailwind, components.json,
index.html) to reference frontend/ paths. Fix .gitignore for
env files and database artifacts. Update all doc path references
to use frontend/ prefix. Regenerate .secrets.baseline.
